package com.vishnu.pdf_studio_api.pdfstudioapi.enums;

/** Which way round a fixed-size page is turned. */
public enum PageOrientation {
    /** Follow the artwork: a wide image gets a landscape page, a tall one portrait. */
    AUTO,
    PORTRAIT,
    LANDSCAPE
}
