package com.vishnu.pdf_studio_api.pdfstudioapi.enums;

/** The free-credit grants that are rationed per client IP as well as per account. */
public enum GrantKind {
    /** One-time credits given when an account is first seen. */
    WELCOME,
    /** The once-a-day free allowance. */
    DAILY
}
