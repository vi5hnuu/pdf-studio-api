package com.vishnu.pdf_studio_api.pdfstudioapi.enums;

/** Image filter types for the filter-image endpoint. */
public enum FilterType {
    GRAYSCALE,
    SEPIA,
    SHARPEN,
    BRIGHTNESS,
    CONTRAST,
    VINTAGE
}
